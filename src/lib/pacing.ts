/**
 * 화면별 최소 체류 시간 (초).
 *
 * 읽지도 보지도 않고 다음으로 넘기는 응답을 막는다. 그런 응답은 dwell_ms 로
 * 사후에 걸러낼 수도 있지만, 그건 이미 받은 자료를 버리는 방식이다 —
 * 애초에 못 넘어가게 하는 편이 표본을 지킨다.
 *
 * 문항에 답하는 것 자체는 언제든 할 수 있고, 막히는 것은 '제출' 뿐이다.
 * /dev 미리보기에서는 기다리지 않는다 (화면을 훑어보는 용도라 매번 기다릴 이유가 없다).
 */
export const MIN_DWELL_SECONDS = {
  /** 3단계 안내 — 이용조건 조작을 전달하는 화면이라 읽고 넘어가야 한다 */
  brief: 5,
  /** 자극물 화면 — 목업을 실제로 보게 하려는 것 */
  stimulus: 10,
} as const;
